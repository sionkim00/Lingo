import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },

  icon: {
    height: 20,
    width: 20,
  },
  lives: {
    color: "red",
    fontWeight: "bold",
  },
});
export default styles;
